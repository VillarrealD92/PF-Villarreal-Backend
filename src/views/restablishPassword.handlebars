<main class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="text-center mb-4"> Restablish Password <i class="fas fa-key"></i></h1>
            <div class="password-form-container p-4 animate__animated animate__fadeIn">
                <p class="password-text mb-4">An email will be sent to your mail address with a link that you can access
                    in order to set a new password to your account. This link will only be accessible for 1 hour.</p>
                <form action="/api/session/mailpassword" method="POST">
                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input id="email" name="email" type="email" class="form-control" required>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary password-btn-send"><i
                                class="fas fa-paper-plane"></i> Send</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</main>